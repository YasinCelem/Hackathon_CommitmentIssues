# Hackathon_CommitmentIssues

from the source:

# 1) create venv in the root
py -3 -m venv .venv

# 2) activate it
. .\.venv\Scripts\Activate.ps1

# 3) upgrade pip and install flask (and whatever else)
python -m pip install --upgrade pip
pip install Flask
pip install PyPDF2
pip install openai
pip install dotenv
pip install google-api-core google-api-python-client google-auth google-auth-httplib2 google-auth-oauthlib googleapis-common-protos
pip install pymupdf
